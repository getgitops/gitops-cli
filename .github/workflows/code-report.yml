name: Code Report

on:
  push:
    branches: [development, main]
  pull_request:

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Install GitOps CLI
        run: bun install --global @getgitops/cli

      - name: Run Code Report scan
        env:
          API_URL: https://cloud.getgitops.com/api
          API_KEY: ${{ secrets.GITOPS_API_KEY }}
        run: >-
          gops creport scan
          --api-url "$API_URL"
          --api-key "$API_KEY"
          --service gitops
          --project gitops
          --git-branch "${GITHUB_REF_NAME}"
          --git-repo-url "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}"
          --git-author "${GITHUB_ACTOR}"
          --git-version "${GITHUB_REF}"
          --git-commit "${GITHUB_SHA}"
          --tags github-actions ci